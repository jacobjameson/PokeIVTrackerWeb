rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow Jacob and Dallas to read/write their own data
    match /users/{userId}/{document=**} {
      allow read, write: if userId in ['jacob', 'dallas'];
    }
  }
}
